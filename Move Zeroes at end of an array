def pushZerosAtEnd(arr):
   
    ar = len(arr);
    count = 0;
    for i in range(len(arr)):
        if(arr[i] != 0):
            arr[i],arr[count] = arr[count],arr[i]
            count += 1

if __name__ =="__main__":
    arr = {1,0,8,7,0,6,5,0};
    # result = {1,8,7,6,5,0,0,0};
